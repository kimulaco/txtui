---
title: Code Block
description: Styled code display blocks with optional language labels and line numbers.
tags:
  - data-display
---

import UIEditor from "../../components/UIEditor.astro";

## Basic

<UIEditor uiId="code-block" uiBlockId="code-block-basic">{`
┌──────────────────────────────┐
│ const x = 42;                │
│ console.log(x);              │
└──────────────────────────────┘
`}</UIEditor>

## With Language

<UIEditor uiId="code-block" uiBlockId="code-block-with-language">{`
┌─ javascript ─────────────────┐
│ const x = 42;                │
│ console.log(x);              │
└──────────────────────────────┘
`}</UIEditor>

## With Line Numbers

<UIEditor uiId="code-block" uiBlockId="code-block-with-line-numbers">{`
┌──────────────────────────────┐
│ 1 │ const x = 42;            │
│ 2 │ const y = 10;            │
│ 3 │ console.log(x + y);      │
└──────────────────────────────┘
`}</UIEditor>
